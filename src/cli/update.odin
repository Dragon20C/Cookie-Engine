package cli
// update script.


update_engine :: proc() {
	// Curl engine release number
	// Compare version numbers
	// if release number is bigger then currently installed
	// replace currently installed with newer version
}
